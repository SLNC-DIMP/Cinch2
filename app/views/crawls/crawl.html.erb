<h1>Enter a Url to crawl:</h1>
<%= form_tag('upload', {id: 'upload_form'}) do %>
    <%= label_tag(:crawl_url, "Enter a Url to crawl: ") %>
    <%= text_field_tag(:crawl_url) %>
    <%= render 'layouts/form-convert' %>
    <br /><br />
    <%= submit_tag 'Submit' %>
<% end %>
